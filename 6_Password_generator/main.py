import random

def generate_password(length=12, use_upper=True, use_lower=True, use_digits=True, use_special=True):
    """Generador de contraseñas seguras"""
    # Definimos los posibles caracteres para cada categoría
    upper_chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    lower_chars = "abcdefghijklmnopqrstuvwxyz"
    digit_chars = "0123456789"
    special_chars = "!@#$%^&*()-_=+[]{}|;:,.<>?/`~"

    # Construimos el conjunto de caracteres según las opciones seleccionadas
    

    # Validamos que al menos una categoría esté seleccionada
    
    # Generamos la contraseña asegurando que al menos un carácter de cada tipo seleccionado esté presente
    
    # Rellenamos el resto de la contraseña con caracteres aleatorios del conjunto completo
  
    # Mezclamos los caracteres para evitar patrones predecibles

    return password